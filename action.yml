name: 'Setup premake'
description: 'Install premake'
inputs:
  version:
    description: 'Premake version'
    required: true
branding:
  icon: 'box'
  color: 'red'
runs:
  using: 'node16'
  main: 'index.js'
